export enum Language {
    PL = 'pl',
}

export enum EventType {
    STATIONARY = 'stationary',
    REMOTE = 'remote',
    HYBRID = 'hybrid',
    CUSTOM = 'custom',
}

export enum Theme {
    LIGHT = 'light',
    DARK = 'dark',
    SYSTEM = 'system',
}
